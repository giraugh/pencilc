fn main() {
  // Define variables
  let x: int = 1;
  let y = 2 * 10;
  let s = "hi world";

  // Compute sum
  let sum = 10 + other(x, y + 1);

  // Print it out
  print(sum)
}

fn other(a: int, b: int) -> int {
  a + b
}

